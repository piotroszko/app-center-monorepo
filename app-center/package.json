{
  "name": "app-center",
  "license": "MIT",
  "version": "1.4.2",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui": "npm run -w @repo/ui ui --",
    "tidy": "turbo tidy",
    "front:push": "turbo front:push"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.3.3",
    "turbo": "2.1.1"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "./apps/*",
    "./packages/*"
  ]
}
